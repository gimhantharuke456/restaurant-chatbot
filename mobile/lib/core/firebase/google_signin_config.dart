/// The "Web client (auto created by Google Service)" OAuth client ID for
/// the spare-parts-6c6af Firebase project — the same project used by the
/// backend and web frontend. Required by google_sign_in v7 on Android so
/// the ID token it returns carries the right audience for Firebase to
/// accept. Not a secret — it's also embedded in google-services.json.
class GoogleSignInConfig {
  GoogleSignInConfig._();

  static const String serverClientId =
      '245028387294-ipupfoupfs9mo33nk83ujh4t0t6stmqd.apps.googleusercontent.com';
}
